// Loading.js
import React from 'react';

const Loading = () => {
    return (
        <div className="loading-spinner">
            <h4>Loading...</h4>
            {/* You can replace this with a spinner or animation if desired */}
        </div>
    );
};

export default Loading;